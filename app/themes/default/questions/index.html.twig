{% extends _layout %}

{% block content %}
	
	<table class="table table-striped">
	{% for question in group.questions().get() %}
	    <tr>
	    	<td>{{ question.order }}</td>
	    	<td>{{ anchor(question.url, question.title) }}</td>
	    </tr>
	{% endfor %}
	</table>

{% endblock %}